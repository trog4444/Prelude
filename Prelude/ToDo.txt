Strings:

lookup all escape characters and add them to Strings escapeChars function
^^ test two versions of esc char for speed and accuracy

change split.byCharWithL to 
byCharRemoveEmptyL? cause thats the only
option thats supported

parse -- however, gives false yesses when given a number.

strings (add desciption in xml for greater, proceeds, etc as to what does what ie A>a or a>1)

(add desciption in xml for greater, proceeds, etc as to what does what ie A>a or a>1)

rename variables to be more descriptive

make unzip in seq lazy


math -> deriv and integr -> make dx/sections before f?

make sure all NEW and old funcs are covered in tests